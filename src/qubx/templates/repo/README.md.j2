# {{ strategy_name }}

{{ description }}

## Setup

```bash
just install
```

## Usage

```bash
# Run tests
just test

# Run strategy in paper mode
uv run qubx run configs/sample.yml --paper

# Install Jupyter kernel
just install-kernel
```
